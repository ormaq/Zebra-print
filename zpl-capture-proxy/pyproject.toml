[project]
name = "zpl-capture-proxy"
version = "3.0.0"
description = "TCP ZPL capture, PNG rendering, and optional reprint/forward workflow"
requires-python = ">=3.10"
dependencies = [
]

[project.scripts]
zpl-capture-proxy = "zpl_capture_proxy.cli:main"

[tool.setuptools.package-data]
zpl_capture_proxy = ["printer_info.json"]
