[project]
name = "zpl-image-converter"
version = "3.0.0"
description = "Pure offline ZPL-to-image conversion package"
requires-python = ">=3.10"
dependencies = [
    "Pillow>=10",
]

[project.scripts]
zpl-to-image = "zpl_image_converter.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
